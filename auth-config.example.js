// Public client configuration template. Never place service-role keys or OAuth client secrets here.
// Populate only after creating/configuring the Supabase project. Auth stays disabled until configured.
window.NEONATAL_GIR_AUTH_CONFIG = Object.freeze({
  supabaseUrl: "",
  supabaseAnonKey: ""
});
